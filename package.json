{
  "name": "agentic-trader-monorepo",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "web:dev": "cd frontend && npm run dev",
    "web:build": "cd frontend && npm run build",
    "web:lint": "cd frontend && npm run lint",
    "desktop:dev": "cd desktop && npm run tauri:dev",
    "desktop:build": "cd desktop && npm run build",
    "desktop:dmg": "cd desktop && npm run tauri:dmg",
    "desktop:tauri-build": "cd desktop && npm run tauri:build"
  },
  "workspaces": [
    "frontend",
    "desktop"
  ]
}
